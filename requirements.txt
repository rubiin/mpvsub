# Python dependencies (pip install -r requirements.txt)
#
# PyGObject (GTK4 + libadwaita bindings) is a system package on most distros
# and cannot be pip-installed reliably. Install it via your package manager:
#   Debian/Ubuntu:  python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
#   Fedora:         python3-gobject gtk4 libadwaita
#   Arch:           python-gobject gtk4 libadwaita
#
# The subtitle backend talks directly to the OpenSubtitles REST API
# (api.opensubtitles.com/api/v1) using only the Python standard library
# (urllib) — no subliminal, no babelfish, no HTTP client dependency.
guessit>=3.0
